// Control de flujo for
for (let i = 0; i < 10; i++) {
  console.log(i);
}

// Acceder a elementos de un array usando "for"
const numbers = [1, 2, "Hola", 4, 5];

console.log(numbers[2]);

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
